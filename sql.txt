
CREATE TABLE users (
	userID INT UNSIGNED NOT NULL AUTO_INCREMENT,
    username VARCHAR(20) NOT NULL,
    password VARCHAR(128) NOT NULL,
    email VARCHAR(50) NOT NULL,
    phone BIGINT NOT NULL,
    status BOOLEAN NOT NULL,
    activationCode MEDIUMINT NOT NULL,
    PRIMARY KEY (userID),
    UNIQUE (username)
);

/*
CREATE TABLE laptops (
	laptopID INT UNSIGNED NOT NULL AUTO_INCREMENT,
    userID INT UNSIGNED NOT NULL,
    brand VARCHAR(20) NOT NULL,
    launchDate YEAR NOT NULL,
    cpuBrand ENUM('INTEL', 'AMD') NOT NULL,
    cpuModel VARCHAR(15) NOT NULL,
    cpuCores ENUM('1', '2', '4', '6', '8') NOT NULL,
    cpuFrequency DECIMAL(3,2) NOT NULL,
    ramSize ENUM('1GB', '1.5GB', '2GB', '4GB', '6GB', '8GB', '12GB', '16GB', '32GB') NOT NULL,
    storageSize SET('128GB SSD', '256GB SSD', '480GB SSD', '512GB SSD', '500GB HDD', '1TB HDD', '2TB HDD') NOT NULL,
    damage BOOLEAN NOT NULL,
    os SET('Windows XP/Vista', 'Windows 7', 'Windows 8/8.1', 'Windows 10', 'Linux', 'macOS', 'Chrome OS', 'No OS') NOT NULL,
    price TINYINT NOT NULL,
    dateOfUpdate TIMESTAMP,
    PRIMARY KEY (laptopID),
    FOREIGN KEY (userID) REFERENCES users(userID)
);
*/

#INSERT INTO users (username, password, email, phone) VALUES ('dimisgan', '1997', 'dimitrisgan97@gmail.com', '6974410860');

#SELECT email FROM users WHERE email = 'dimitrisgan97@gmail.com';


